name: VC Sourcing Pipeline

on:
  schedule:
    - cron: '30 2 * * *'   # 2:30 AM UTC = 8 AM IST daily
  workflow_dispatch:         # manual trigger from GitHub UI

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write        # needed to commit data.json back

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run pipeline
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          CEREBRAS_API_KEY: ${{ secrets.CEREBRAS_API_KEY }}
          ZHIPU_API_KEY: ${{ secrets.ZHIPU_API_KEY }}
          SAMBANOVA_API_KEY: ${{ secrets.SAMBANOVA_API_KEY }}
          EXA_API_KEY: ${{ secrets.EXA_API_KEY }}
          SERPER_API_KEY: ${{ secrets.SERPER_API_KEY }}
          BRAVE_API_KEY: ${{ secrets.BRAVE_API_KEY }}
          GOOGLE_CSE_API_KEY: ${{ secrets.GOOGLE_CSE_API_KEY }}
          GOOGLE_CSE_CX: ${{ secrets.GOOGLE_CSE_CX }}
          TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
          MIN_SCORE_THRESHOLD: "40"   # raised from 30: rule-based floor is ~30, which let noise through
        run: python scripts/run_pipeline.py

      - name: Commit updated data.json
        run: |
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git add docs/data.json
          git diff --staged --quiet && echo "No changes" || (git commit -m "chore: pipeline run $(date -u +%Y-%m-%dT%H:%M:%SZ) [skip ci]" && git pull --rebase origin main && git push)
